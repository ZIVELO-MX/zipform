openapi: 3.1.0
info:
  title: Zipform Data API
  version: 1.3.0-draft
  description: |
    Full CRUD access to Zipform platform data: users, projects, missions,
    seasons, episodes, quest items, resources, agents, and API keys.

    Authentication is via Bearer token (agent API key) or browser session cookie.
    If both are present, the Bearer token takes priority.
  contact:
    name: Zivelo Platform
  license:
    name: Proprietary
    identifier: LicenseRef-Zivelo-Proprietary
servers:
  - url: /api/v1
    description: Current Zipform deployment
security:
  - bearerToken: []
  - sessionCookie: []
tags:
  - name: Users
    description: User profile data (no passwords or sessions exposed).
  - name: Agents
    description: Automated agent accounts that act on behalf of the platform.
  - name: API Keys
    description: API key management for agent authentication.
  - name: Projects
    description: TLOZ project metadata with full CRUD.
  - name: Missions
    description: Missions with filters, detail, and full CRUD.
  - name: Seasons
    description: TLOZ seasons.
  - name: Episodes
    description: TLOZ episodes.
  - name: Quest Items
    description: Inventory / quest items with full CRUD.
  - name: Resources
    description: Attached resources (docs, links, notes) with full CRUD.
paths:
  # ── Users ──────────────────────────────────────────────────────────
  /users:
    get:
      operationId: listUsers
      summary: List or find users
      description: Filters are exact and case-insensitive. Only one filter at a time.
      tags: [Users]
      parameters:
        - $ref: "#/components/parameters/Limit"
        - $ref: "#/components/parameters/Cursor"
        - name: email
          in: query
          description: Exact email address.
          schema:
            type: string
            format: email
        - name: username
          in: query
          description: Exact username.
          schema:
            type: string
            minLength: 1
            maxLength: 64
      responses:
        "200":
          description: Matching users.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/UserListResponse"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "500":
          $ref: "#/components/responses/InternalError"
    post:
      operationId: queryUsers
      summary: Query users by example
      description: Like GET /users but filters are sent in the request body.
      tags: [Users]
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                email:
                  type: string
                  format: email
                username:
                  type: string
                limit:
                  type: integer
                  minimum: 1
                  maximum: 100
                  default: 25
                cursor:
                  type: string
      responses:
        "200":
          description: Matching users.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/UserListResponse"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "500":
          $ref: "#/components/responses/InternalError"
  /users/me:
    get:
      operationId: getCurrentUser
      summary: Get the authenticated user
      description: Resolves the user associated with the Bearer API key or active browser session.
      tags: [Users]
      responses:
        "200":
          description: Authenticated public user profile.
          content:
            application/json:
              schema:
                type: object
                required: [data]
                properties:
                  data:
                    $ref: "#/components/schemas/User"
        "401":
          $ref: "#/components/responses/Unauthorized"
  /users/{userId}:
    get:
      operationId: getUser
      summary: Get a user by ID
      tags: [Users]
      parameters:
        - $ref: "#/components/parameters/UserId"
      responses:
        "200":
          description: User found.
          content:
            application/json:
              schema:
                type: object
                required: [data]
                properties:
                  data:
                    $ref: "#/components/schemas/User"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "404":
          $ref: "#/components/responses/NotFound"
        "500":
          $ref: "#/components/responses/InternalError"

  # ── Projects ───────────────────────────────────────────────────────
  /projects:
    get:
      operationId: listProjects
      summary: List TLOZ projects
      tags: [Projects]
      parameters:
        - $ref: "#/components/parameters/Limit"
        - $ref: "#/components/parameters/Cursor"
        - name: ownerId
          in: query
          schema:
            type: string
            minLength: 1
            maxLength: 128
        - name: status
          in: query
          schema:
            $ref: "#/components/schemas/ProjectStatus"
      responses:
        "200":
          description: Matching projects.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ProjectListResponse"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "500":
          $ref: "#/components/responses/InternalError"
    post:
      operationId: createProject
      summary: Create a TLOZ project
      description: name is required; all other fields are optional.
      tags: [Projects]
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [name]
              properties:
                name:
                  type: string
                description:
                  type: string
                descriptionDetail:
                  type: string
                icon:
                  type: string
                color:
                  type: string
                  pattern: "^#[0-9a-fA-F]{6}$"
                status:
                  $ref: "#/components/schemas/ProjectStatus"
                type:
                  type: string
                ownerId:
                  type: string
                startDate:
                  type: string
                  format: date
                dueDate:
                  type: string
                  format: date
      responses:
        "201":
          description: Project created.
          content:
            application/json:
              schema:
                type: object
                required: [data]
                properties:
                  data:
                    $ref: "#/components/schemas/Project"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "500":
          $ref: "#/components/responses/InternalError"
  /projects/query:
    post:
      operationId: queryProjects
      summary: Query projects by example
      description: Like GET /projects but filters are sent in the request body.
      tags: [Projects]
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                ownerId:
                  type: string
                status:
                  $ref: "#/components/schemas/ProjectStatus"
                limit:
                  type: integer
                  minimum: 1
                  maximum: 100
                  default: 25
                cursor:
                  type: string
      responses:
        "200":
          description: Matching projects.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ProjectListResponse"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "500":
          $ref: "#/components/responses/InternalError"
  /projects/{projectId}:
    get:
      operationId: getProject
      summary: Get a TLOZ project by ID
      tags: [Projects]
      parameters:
        - $ref: "#/components/parameters/ProjectId"
      responses:
        "200":
          description: Project found.
          content:
            application/json:
              schema:
                type: object
                required: [data]
                properties:
                  data:
                    $ref: "#/components/schemas/Project"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "404":
          $ref: "#/components/responses/NotFound"
        "500":
          $ref: "#/components/responses/InternalError"
    patch:
      operationId: updateProject
      summary: Update a TLOZ project
      tags: [Projects]
      parameters:
        - $ref: "#/components/parameters/ProjectId"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                name:
                  type: string
                description:
                  type: string
                descriptionDetail:
                  type: string
                icon:
                  type: string
                color:
                  type: string
                  pattern: "^#[0-9a-fA-F]{6}$"
                status:
                  $ref: "#/components/schemas/ProjectStatus"
                type:
                  type: string
                ownerId:
                  type: string
                startDate:
                  type: string
                  format: date
                dueDate:
                  type: string
                  format: date
      responses:
        "200":
          description: Project updated.
          content:
            application/json:
              schema:
                type: object
                required: [data]
                properties:
                  data:
                    $ref: "#/components/schemas/Project"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "404":
          $ref: "#/components/responses/NotFound"
        "500":
          $ref: "#/components/responses/InternalError"

  # ── Missions ───────────────────────────────────────────────────────
  /missions:
    get:
      operationId: listMissions
      summary: List or find missions
      description: Supports pagination, text search on title, and filtering by project, owner, status, season, and episode.
      tags: [Missions]
      parameters:
        - $ref: "#/components/parameters/Limit"
        - $ref: "#/components/parameters/Cursor"
        - name: projectId
          in: query
          schema:
            type: string
        - name: ownerId
          in: query
          schema:
            type: string
        - name: status
          in: query
          schema:
            $ref: "#/components/schemas/MissionStatus"
        - name: seasonId
          in: query
          schema:
            type: string
        - name: episodeId
          in: query
          schema:
            type: string
        - name: title
          in: query
          description: Case-insensitive partial text search on title.
          schema:
            type: string
      responses:
        "200":
          description: Matching missions with hydrated relations.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/MissionListResponse"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "500":
          $ref: "#/components/responses/InternalError"
    post:
      operationId: createMission
      summary: Create a mission
      description: title, ownerId, type, and projectId are required; stable defaults are applied to omitted presentation and progress fields.
      tags: [Missions]
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [title, ownerId, type, projectId]
              properties:
                id:
                  type: string
                  description: Optional custom ID (auto-generated if omitted).
                title:
                  type: string
                description:
                  type: string
                  default: ""
                icon:
                  type: string
                  default: Sword
                type:
                  $ref: "#/components/schemas/MissionType"
                status:
                  $ref: "#/components/schemas/MissionStatus"
                  default: next
                conclusion:
                  type: string
                ownerId:
                  type: string
                projectId:
                  type: string
                seasonId:
                  type: string
                episodeId:
                  type: string
                dueDate:
                  type: string
                  format: date
                startDate:
                  type: string
                  format: date
                completedAt:
                  type: string
                  format: date-time
                blockedReason:
                  type: string
                progress:
                  type: integer
                  minimum: 0
                  maximum: 100
                  default: 0
      responses:
        "201":
          description: Mission created.
          content:
            application/json:
              schema:
                type: object
                required: [data]
                properties:
                  data:
                    $ref: "#/components/schemas/Mission"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "500":
          $ref: "#/components/responses/InternalError"
  /missions/query:
    post:
      operationId: queryMissions
      summary: Query missions by example
      description: Like GET /missions but filters are sent in the request body.
      tags: [Missions]
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                projectId:
                  type: string
                ownerId:
                  type: string
                status:
                  $ref: "#/components/schemas/MissionStatus"
                seasonId:
                  type: string
                episodeId:
                  type: string
                title:
                  type: string
                limit:
                  type: integer
                  minimum: 1
                  maximum: 100
                  default: 25
                cursor:
                  type: string
      responses:
        "200":
          description: Matching missions.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/MissionListResponse"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "500":
          $ref: "#/components/responses/InternalError"
  /missions/{missionId}:
    get:
      operationId: getMissionDetail
      summary: Get mission detail by ID
      tags: [Missions]
      parameters:
        - $ref: "#/components/parameters/MissionId"
      responses:
        "200":
          description: Mission detail with checklist, resources, dependencies.
          content:
            application/json:
              schema:
                type: object
                required: [data]
                properties:
                  data:
                    $ref: "#/components/schemas/MissionDetail"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "404":
          $ref: "#/components/responses/NotFound"
        "500":
          $ref: "#/components/responses/InternalError"
    patch:
      operationId: updateMission
      summary: Update mission fields
      description: Only the provided fields are updated. Valid fields: title, description, icon, type, status, conclusion, dueDate, startDate, completedAt, blockedReason, progress, seasonId, episodeId, ownerId, projectId.
      tags: [Missions]
      parameters:
        - $ref: "#/components/parameters/MissionId"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                title:
                  type: string
                description:
                  type: string
                icon:
                  type: string
                type:
                  $ref: "#/components/schemas/MissionType"
                status:
                  $ref: "#/components/schemas/MissionStatus"
                conclusion:
                  type: string
                dueDate:
                  type: string
                  format: date
                startDate:
                  type: string
                  format: date
                completedAt:
                  type: string
                  format: date-time
                blockedReason:
                  type: string
                progress:
                  type: integer
                  minimum: 0
                  maximum: 100
                seasonId:
                  type: string
                episodeId:
                  type: string
                ownerId:
                  type: string
                projectId:
                  type: string
      responses:
        "200":
          description: Mission updated.
          content:
            application/json:
              schema:
                type: object
                required: [data]
                properties:
                  data:
                    $ref: "#/components/schemas/Mission"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "404":
          $ref: "#/components/responses/NotFound"
        "500":
          $ref: "#/components/responses/InternalError"
    delete:
      operationId: deleteMission
      summary: Delete a mission
      tags: [Missions]
      parameters:
        - $ref: "#/components/parameters/MissionId"
      responses:
        "200":
          description: Mission deleted.
          content:
            application/json:
              schema:
                type: object
                required: [success]
                properties:
                  success:
                    type: boolean
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "404":
          $ref: "#/components/responses/NotFound"
        "500":
          $ref: "#/components/responses/InternalError"
  /missions/{missionId}/status:
    patch:
      operationId: patchMissionStatus
      summary: Change mission status only
      description: Dedicated endpoint to update just the status field.
      tags: [Missions]
      parameters:
        - $ref: "#/components/parameters/MissionId"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [status]
              properties:
                status:
                  $ref: "#/components/schemas/MissionStatus"
      responses:
        "200":
          description: Status updated.
          content:
            application/json:
              schema:
                type: object
                required: [data]
                properties:
                  data:
                    $ref: "#/components/schemas/Mission"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "404":
          $ref: "#/components/responses/NotFound"
        "500":
          $ref: "#/components/responses/InternalError"
  /missions/{missionId}/dependencies:
    post:
      operationId: addMissionDependency
      summary: Add a dependency to a mission
      tags: [Missions]
      parameters:
        - $ref: "#/components/parameters/MissionId"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [dependsOnMissionId]
              properties:
                dependsOnMissionId:
                  type: string
                  description: ID of the mission this mission depends on.
      responses:
        "200":
          description: Dependency added. Returns updated mission detail.
          content:
            application/json:
              schema:
                type: object
                required: [data]
                properties:
                  data:
                    $ref: "#/components/schemas/MissionDetail"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "404":
          $ref: "#/components/responses/NotFound"
        "500":
          $ref: "#/components/responses/InternalError"
  /missions/{missionId}/dependencies/{dependencyId}:
    delete:
      operationId: removeMissionDependency
      summary: Remove a dependency from a mission
      tags: [Missions]
      parameters:
        - $ref: "#/components/parameters/MissionId"
        - name: dependencyId
          in: path
          required: true
          schema:
            type: string
            minLength: 1
            maxLength: 128
      responses:
        "200":
          description: Dependency removed. Returns updated mission detail.
          content:
            application/json:
              schema:
                type: object
                required: [data]
                properties:
                  data:
                    $ref: "#/components/schemas/MissionDetail"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "404":
          $ref: "#/components/responses/NotFound"
        "500":
          $ref: "#/components/responses/InternalError"
  /missions/{missionId}/resources:
    post:
      operationId: addMissionResource
      summary: Add a resource to a mission
      tags: [Resources]
      parameters:
        - $ref: "#/components/parameters/MissionId"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [type, title]
              properties:
                type:
                  $ref: "#/components/schemas/ResourceType"
                title:
                  type: string
                url:
                  type: string
                  format: uri
                fileId:
                  type: string
      responses:
        "200":
          description: Resource added. Returns updated mission detail.
          content:
            application/json:
              schema:
                type: object
                required: [data]
                properties:
                  data:
                    $ref: "#/components/schemas/MissionDetail"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "404":
          $ref: "#/components/responses/NotFound"
        "500":
          $ref: "#/components/responses/InternalError"
  /missions/{missionId}/resources/{resourceId}:
    delete:
      operationId: removeMissionResource
      summary: Remove a resource from a mission
      tags: [Resources]
      parameters:
        - $ref: "#/components/parameters/MissionId"
        - name: resourceId
          in: path
          required: true
          schema:
            type: string
            minLength: 1
            maxLength: 128
      responses:
        "200":
          description: Resource removed. Returns updated mission detail.
          content:
            application/json:
              schema:
                type: object
                required: [data]
                properties:
                  data:
                    $ref: "#/components/schemas/MissionDetail"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "404":
          $ref: "#/components/responses/NotFound"
        "500":
          $ref: "#/components/responses/InternalError"
  /missions/{missionId}/quest-items:
    post:
      operationId: linkMissionQuestItem
      summary: Link a quest item to a mission
      tags: [Missions]
      parameters:
        - $ref: "#/components/parameters/MissionId"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [questItemId]
              properties:
                questItemId:
                  type: string
                required:
                  type: boolean
                  description: Whether the quest item is required for this mission.
                  default: false
      responses:
        "200":
          description: Quest item linked. Returns updated mission detail.
          content:
            application/json:
              schema:
                type: object
                required: [data]
                properties:
                  data:
                    $ref: "#/components/schemas/MissionDetail"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "404":
          $ref: "#/components/responses/NotFound"
        "500":
          $ref: "#/components/responses/InternalError"
  /missions/{missionId}/quest-items/{questItemId}:
    delete:
      operationId: unlinkMissionQuestItem
      summary: Unlink a quest item from a mission
      tags: [Missions]
      parameters:
        - $ref: "#/components/parameters/MissionId"
        - name: questItemId
          in: path
          required: true
          schema:
            type: string
            minLength: 1
            maxLength: 128
      responses:
        "200":
          description: Quest item unlinked. Returns updated mission detail.
          content:
            application/json:
              schema:
                type: object
                required: [data]
                properties:
                  data:
                    $ref: "#/components/schemas/MissionDetail"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "404":
          $ref: "#/components/responses/NotFound"
        "500":
          $ref: "#/components/responses/InternalError"
  /missions/{missionId}/document:
    put:
      operationId: saveMissionDocument
      summary: Save mission document (markdown with checklist)
      description: The document text is parsed for embedded checklist items.
      tags: [Missions]
      parameters:
        - $ref: "#/components/parameters/MissionId"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [markdown]
              properties:
                markdown:
                  type: string
                  description: Full markdown content for the mission document. Checklist items are parsed from `- [ ]` / `- [x]` syntax.
      responses:
        "200":
          description: Document saved. Returns updated mission detail.
          content:
            application/json:
              schema:
                type: object
                required: [data]
                properties:
                  data:
                    $ref: "#/components/schemas/MissionDetail"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "404":
          $ref: "#/components/responses/NotFound"
        "500":
          $ref: "#/components/responses/InternalError"

  # ── Seasons ────────────────────────────────────────────────────────
  /seasons:
    get:
      operationId: listSeasons
      summary: List all seasons
      tags: [Seasons]
      responses:
        "200":
          description: All seasons.
          content:
            application/json:
              schema:
                type: object
                required: [data]
                properties:
                  data:
                    type: array
                    items:
                      $ref: "#/components/schemas/Season"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "500":
          $ref: "#/components/responses/InternalError"

  # ── Episodes ───────────────────────────────────────────────────────
  /episodes:
    get:
      operationId: listEpisodes
      summary: List episodes (optional filter by seasonId)
      tags: [Episodes]
      parameters:
        - name: seasonId
          in: query
          schema:
            type: string
      responses:
        "200":
          description: Matching episodes.
          content:
            application/json:
              schema:
                type: object
                required: [data]
                properties:
                  data:
                    type: array
                    items:
                      $ref: "#/components/schemas/Episode"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "500":
          $ref: "#/components/responses/InternalError"

  # ── Quest Items ────────────────────────────────────────────────────
  /quest-items:
    get:
      operationId: listQuestItems
      summary: List or find quest items
      tags: [Quest Items]
      parameters:
        - $ref: "#/components/parameters/Limit"
        - $ref: "#/components/parameters/Cursor"
        - name: ownerId
          in: query
          schema:
            type: string
        - name: status
          in: query
          schema:
            $ref: "#/components/schemas/InventoryStatus"
        - name: category
          in: query
          schema:
            $ref: "#/components/schemas/InventoryCategory"
      responses:
        "200":
          description: Matching quest items.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/QuestItemListResponse"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "500":
          $ref: "#/components/responses/InternalError"
    post:
      operationId: createQuestItem
      summary: Create a quest item
      description: name is required; all other fields are optional.
      tags: [Quest Items]
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [name]
              properties:
                name:
                  type: string
                description:
                  type: string
                descriptionDetail:
                  type: string
                icon:
                  type: string
                status:
                  $ref: "#/components/schemas/InventoryStatus"
                category:
                  $ref: "#/components/schemas/InventoryCategory"
                ownerId:
                  type: string
                acquiredAt:
                  type: string
                  format: date
      responses:
        "201":
          description: Quest item created.
          content:
            application/json:
              schema:
                type: object
                required: [data]
                properties:
                  data:
                    $ref: "#/components/schemas/QuestItem"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "500":
          $ref: "#/components/responses/InternalError"
  /quest-items/query:
    post:
      operationId: queryQuestItems
      summary: Query quest items by example
      description: Like GET /quest-items but filters are sent in the request body.
      tags: [Quest Items]
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                ownerId:
                  type: string
                status:
                  $ref: "#/components/schemas/InventoryStatus"
                category:
                  $ref: "#/components/schemas/InventoryCategory"
                limit:
                  type: integer
                  minimum: 1
                  maximum: 100
                  default: 25
                cursor:
                  type: string
      responses:
        "200":
          description: Matching quest items.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/QuestItemListResponse"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "500":
          $ref: "#/components/responses/InternalError"
  /quest-items/{questItemId}:
    get:
      operationId: getQuestItem
      summary: Get a quest item by ID
      tags: [Quest Items]
      parameters:
        - name: questItemId
          in: path
          required: true
          schema:
            type: string
            minLength: 1
            maxLength: 128
      responses:
        "200":
          description: Quest item found.
          content:
            application/json:
              schema:
                type: object
                required: [data]
                properties:
                  data:
                    $ref: "#/components/schemas/QuestItem"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "404":
          $ref: "#/components/responses/NotFound"
        "500":
          $ref: "#/components/responses/InternalError"
    patch:
      operationId: updateQuestItem
      summary: Update a quest item
      tags: [Quest Items]
      parameters:
        - name: questItemId
          in: path
          required: true
          schema:
            type: string
            minLength: 1
            maxLength: 128
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                name:
                  type: string
                description:
                  type: string
                descriptionDetail:
                  type: string
                icon:
                  type: string
                status:
                  $ref: "#/components/schemas/InventoryStatus"
                category:
                  $ref: "#/components/schemas/InventoryCategory"
                ownerId:
                  type: string
                acquiredAt:
                  type: string
                  format: date
      responses:
        "200":
          description: Quest item updated.
          content:
            application/json:
              schema:
                type: object
                required: [data]
                properties:
                  data:
                    $ref: "#/components/schemas/QuestItem"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "404":
          $ref: "#/components/responses/NotFound"
        "500":
          $ref: "#/components/responses/InternalError"

  # ── Resources ──────────────────────────────────────────────────────
  /resources:
    get:
      operationId: listResources
      summary: List resources filtered by owner entity or type
      tags: [Resources]
      parameters:
        - $ref: "#/components/parameters/Limit"
        - $ref: "#/components/parameters/Cursor"
        - name: missionId
          in: query
          schema:
            type: string
        - name: projectId
          in: query
          schema:
            type: string
        - name: questItemId
          in: query
          schema:
            type: string
        - name: type
          in: query
          schema:
            $ref: "#/components/schemas/ResourceType"
      responses:
        "200":
          description: Matching resources.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ResourceListResponse"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "500":
          $ref: "#/components/responses/InternalError"
  /resources/query:
    post:
      operationId: queryResources
      summary: Query resources by example
      description: Like GET /resources but filters are sent in the request body.
      tags: [Resources]
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                missionId:
                  type: string
                projectId:
                  type: string
                questItemId:
                  type: string
                type:
                  $ref: "#/components/schemas/ResourceType"
                limit:
                  type: integer
                  minimum: 1
                  maximum: 100
                  default: 25
                cursor:
                  type: string
      responses:
        "200":
          description: Matching resources.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ResourceListResponse"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "500":
          $ref: "#/components/responses/InternalError"

  # ── Agents ─────────────────────────────────────────────────────────
  /agents:
    get:
      operationId: listAgents
      summary: List or find agent accounts
      description: Returns all user profiles where type=agent. These are non-human accounts used for automation.
      tags: [Agents]
      responses:
        "200":
          description: Agent list.
          content:
            application/json:
              schema:
                type: object
                required: [data]
                properties:
                  data:
                    type: array
                    items:
                      $ref: "#/components/schemas/User"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "500":
          $ref: "#/components/responses/InternalError"
    post:
      operationId: createAgent
      summary: Create a new agent account
      description: Creates a non-human user with type=agent and a default API key. The raw key is returned only once.
      tags: [Agents]
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [name, username, email]
              properties:
                name:
                  type: string
                username:
                  type: string
                email:
                  type: string
                  format: email
                role:
                  type: string
                  default: "agent:operative"
      responses:
        "201":
          description: Agent created with default API key.
          content:
            application/json:
              schema:
                type: object
                required: [user, apiKey]
                properties:
                  user:
                    $ref: "#/components/schemas/User"
                  apiKey:
                    $ref: "#/components/schemas/ApiKeyCreateResult"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "500":
          $ref: "#/components/responses/InternalError"
  /agents/{agentId}/api-keys:
    get:
      operationId: listAgentApiKeys
      summary: List API keys for an agent
      tags: [API Keys]
      parameters:
        - name: agentId
          in: path
          required: true
          schema:
            type: string
            minLength: 1
            maxLength: 128
      responses:
        "200":
          description: API keys for the agent.
          content:
            application/json:
              schema:
                type: object
                required: [data]
                properties:
                  data:
                    type: array
                    items:
                      $ref: "#/components/schemas/ApiKey"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "500":
          $ref: "#/components/responses/InternalError"
    post:
      operationId: createAgentApiKey
      summary: Create a new API key for an agent
      description: Generates a new zaf_-prefixed key. The raw key is returned only once.
      tags: [API Keys]
      parameters:
        - name: agentId
          in: path
          required: true
          schema:
            type: string
            minLength: 1
            maxLength: 128
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [name]
              properties:
                name:
                  type: string
      responses:
        "201":
          description: API key created.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiKeyCreateResult"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "500":
          $ref: "#/components/responses/InternalError"
  /agents/{agentId}/api-keys/{keyId}:
    delete:
      operationId: revokeAgentApiKey
      summary: Revoke (delete) an API key
      tags: [API Keys]
      parameters:
        - name: agentId
          in: path
          required: true
          schema:
            type: string
            minLength: 1
            maxLength: 128
        - name: keyId
          in: path
          required: true
          schema:
            type: string
            minLength: 1
            maxLength: 128
      responses:
        "200":
          description: Key revoked.
          content:
            application/json:
              schema:
                type: object
                required: [success]
                properties:
                  success:
                    type: boolean
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "500":
          $ref: "#/components/responses/InternalError"

components:
  securitySchemes:
    sessionCookie:
      type: apiKey
      in: cookie
      name: authjs.session-token
      description: Existing NextAuth browser session. Production may use the secure cookie prefix.
    bearerToken:
      type: http
      scheme: bearer
      bearerFormat: zaf_
      description: |
        Agent API key. Prefix is always `zaf_` followed by 64 hex characters.
        To authenticate, include the header:
        ```
        Authorization: Bearer zaf_<64-hex-chars>
        ```
        If both Bearer token and session cookie are present, the Bearer token takes priority.
  parameters:
    Limit:
      name: limit
      in: query
      schema:
        type: integer
        minimum: 1
        maximum: 100
        default: 25
    Cursor:
      name: cursor
      in: query
      schema:
        type: string
        minLength: 1
    UserId:
      name: userId
      in: path
      required: true
      schema:
        type: string
        minLength: 1
        maxLength: 128
    ProjectId:
      name: projectId
      in: path
      required: true
      schema:
        type: string
        minLength: 1
        maxLength: 128
    MissionId:
      name: missionId
      in: path
      required: true
      schema:
        type: string
        minLength: 1
        maxLength: 128
  responses:
    BadRequest:
      description: Invalid path or query input.
      content:
        application/json:
          schema:
            $ref: "#/components/schemas/ApiError"
    Unauthorized:
      description: An authenticated session or valid API key is required.
      content:
        application/json:
          schema:
            $ref: "#/components/schemas/ApiError"
    NotFound:
      description: The requested resource does not exist.
      content:
        application/json:
          schema:
            $ref: "#/components/schemas/ApiError"
    InternalError:
      description: Unexpected server error. Details are recorded server-side.
      content:
        application/json:
          schema:
            $ref: "#/components/schemas/ApiError"
  schemas:
    # ── Shared ───────────────────────────────────────────────────────
    PageInfo:
      type: object
      additionalProperties: false
      required: [nextCursor]
      properties:
        nextCursor:
          type: [string, "null"]
    ApiError:
      type: object
      additionalProperties: false
      required: [error]
      properties:
        error:
          type: object
          additionalProperties: false
          required: [code, message, requestId]
          properties:
            code:
              type: string
              enum: [INVALID_REQUEST, UNAUTHORIZED, FORBIDDEN, NOT_FOUND, INTERNAL_ERROR]
            message:
              type: string
            fields:
              type: object
              description: Field-specific guidance for INVALID_REQUEST responses.
              additionalProperties:
                type: string
            requestId:
              type: string

    # ── Users ────────────────────────────────────────────────────────
    User:
      type: object
      additionalProperties: false
      description: Public profile. Password hashes, sessions, and other authentication secrets are never exposed.
      required: [id, name, username, email, role, type, avatarUrl]
      properties:
        id:
          type: string
        name:
          type: string
        username:
          type: string
        email:
          type: string
          format: email
        role:
          type: string
        type:
          type: string
          enum: [human, agent]
        avatarUrl:
          type: string
          format: uri
        theme:
          type: string
          enum: [system, light, dark]
    UserListResponse:
      type: object
      additionalProperties: false
      required: [data, page]
      properties:
        data:
          type: array
          items:
            $ref: "#/components/schemas/User"
        page:
          $ref: "#/components/schemas/PageInfo"

    # ── Projects ─────────────────────────────────────────────────────
    ProjectStatus:
      type: string
      enum: [planned, active, archived]
    Project:
      type: object
      additionalProperties: false
      required: [id, slug, name, description, color, icon, status, ownerId, startDate]
      properties:
        id:
          type: string
        slug:
          type: string
        name:
          type: string
        description:
          type: string
        descriptionDetail:
          type: string
        color:
          type: string
          pattern: "^#[0-9a-fA-F]{6}$"
        icon:
          type: string
        status:
          $ref: "#/components/schemas/ProjectStatus"
        ownerId:
          type: string
        startDate:
          type: string
          format: date
        dueDate:
          type: [string, "null"]
          format: date
    ProjectListResponse:
      type: object
      additionalProperties: false
      required: [data, page]
      properties:
        data:
          type: array
          items:
            $ref: "#/components/schemas/Project"
        page:
          $ref: "#/components/schemas/PageInfo"

    # ── Missions ─────────────────────────────────────────────────────
    MissionStatus:
      type: string
      enum: [now, next, later, completed, blocked]
    MissionType:
      type: string
      enum: [main_quest, side_quest, farming_quest, exploration_quest]
    Mission:
      type: object
      additionalProperties: false
      required: [id, displayId, title, description, icon, type, status, ownerId, progress]
      properties:
        id:
          type: string
        displayId:
          type: string
        title:
          type: string
        description:
          type: string
        icon:
          type: string
        type:
          $ref: "#/components/schemas/MissionType"
        status:
          $ref: "#/components/schemas/MissionStatus"
        ownerId:
          type: string
        projectId:
          type: string
        seasonId:
          type: string
        episodeId:
          type: string
        dueDate:
          type: string
          format: date
        progress:
          type: integer
          minimum: 0
          maximum: 100
        completedAt:
          type: string
          format: date-time
        blockedReason:
          type: string
    MissionDetail:
      allOf:
        - $ref: "#/components/schemas/Mission"
        - type: object
          properties:
            project:
              $ref: "#/components/schemas/Project"
            dependencies:
              type: array
              items:
                $ref: "#/components/schemas/Mission"
            questItems:
              type: array
              items:
                $ref: "#/components/schemas/QuestItem"
            checklist:
              type: array
              items:
                $ref: "#/components/schemas/ChecklistItem"
            resources:
              type: array
              items:
                $ref: "#/components/schemas/Resource"
    MissionListResponse:
      type: object
      additionalProperties: false
      required: [data, page]
      properties:
        data:
          type: array
          items:
            $ref: "#/components/schemas/MissionDetail"
        page:
          $ref: "#/components/schemas/PageInfo"

    # ── Seasons ──────────────────────────────────────────────────────
    Season:
      type: object
      additionalProperties: false
      required: [id, name, version, description, status, startDate]
      properties:
        id:
          type: string
        name:
          type: string
        version:
          type: string
        description:
          type: string
        status:
          type: string
        startDate:
          type: string
          format: date
        endDate:
          type: [string, "null"]
          format: date

    # ── Episodes ─────────────────────────────────────────────────────
    Episode:
      type: object
      additionalProperties: false
      required: [id, seasonId, name, romanNumber, description, status, startDate]
      properties:
        id:
          type: string
        seasonId:
          type: string
        name:
          type: string
        romanNumber:
          type: string
        description:
          type: string
        status:
          type: string
        startDate:
          type: string
          format: date
        endDate:
          type: [string, "null"]
          format: date

    # ── Quest Items ──────────────────────────────────────────────────
    InventoryStatus:
      type: string
      enum: [locked, unlocked]
    InventoryCategory:
      type: string
      enum: [tool, access, asset, document, other]
    QuestItem:
      type: object
      additionalProperties: false
      required: [id, name, description, icon, status, category]
      properties:
        id:
          type: string
        name:
          type: string
        description:
          type: string
        icon:
          type: string
        status:
          $ref: "#/components/schemas/InventoryStatus"
        category:
          $ref: "#/components/schemas/InventoryCategory"
        ownerId:
          type: string
        acquiredAt:
          type: string
          format: date
    QuestItemListResponse:
      type: object
      additionalProperties: false
      required: [data, page]
      properties:
        data:
          type: array
          items:
            $ref: "#/components/schemas/QuestItem"
        page:
          $ref: "#/components/schemas/PageInfo"

    # ── Resources ────────────────────────────────────────────────────
    ResourceType:
      type: string
      enum: [link, document, image, file, note]
    Resource:
      type: object
      additionalProperties: false
      required: [id, type, title]
      properties:
        id:
          type: string
        missionId:
          type: string
        projectId:
          type: string
        questItemId:
          type: string
        type:
          $ref: "#/components/schemas/ResourceType"
        title:
          type: string
        url:
          type: string
          format: uri
        fileId:
          type: string
    ResourceListResponse:
      type: object
      additionalProperties: false
      required: [data, page]
      properties:
        data:
          type: array
          items:
            $ref: "#/components/schemas/Resource"
        page:
          $ref: "#/components/schemas/PageInfo"

    # ── API Keys ─────────────────────────────────────────────────────
    ApiKey:
      type: object
      additionalProperties: false
      required: [id, userId, createdByUserId, name, keyPrefix, createdAt]
      properties:
        id:
          type: string
        userId:
          type: string
        createdByUserId:
          type: string
        name:
          type: string
        keyPrefix:
          type: string
        lastUsedAt:
          type: string
          format: date-time
        expiresAt:
          type: string
          format: date-time
        createdAt:
          type: string
          format: date-time
        updatedAt:
          type: string
          format: date-time
    ApiKeyCreateResult:
      type: object
      additionalProperties: false
      required: [key, apiKey]
      properties:
        key:
          type: string
          description: "Raw API key (shown only once, begins with zaf_)"
        apiKey:
          $ref: "#/components/schemas/ApiKey"

    # ── Checklist Items ──────────────────────────────────────────────
    ChecklistItem:
      type: object
      additionalProperties: false
      required: [id, missionId, title, completed, position]
      properties:
        id:
          type: string
        missionId:
          type: string
        title:
          type: string
        completed:
          type: boolean
        position:
          type: integer
