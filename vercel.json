{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "framework": "nextjs",
  "installCommand": "pnpm install --frozen-lockfile",
  "buildCommand": "pnpm db:generate && pnpm --filter @zipform/dashboard build",
  "devCommand": "pnpm dev",
  "outputDirectory": "apps/dashboard/.next"
}
